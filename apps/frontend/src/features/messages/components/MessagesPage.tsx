// Both message routes intentionally resolve to the same component identity so
// switching between inbox and conversation does not remount the split pane.
export { default } from './MessagesWorkspace'
